import 'package:flutter/material.dart';

class TasksMatrixScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tasks Matrix'));
  }
}
